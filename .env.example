# 数据库连接配置示例（请复制此文件为 .env 并填写真实值）
DB_HOST=你的数据库地址（本地填localhost，Render填MySQL主机地址）
DB_USER=你的数据库用户名（如root）
DB_PASSWORD=你的数据库密码（本地填123456，部署填Render MySQL密码）
DB_NAME=test_db（固定不变）
DB_PORT=3306（固定不变）
DB_CHARSET=utf8mb4（固定不变）
DB_AUTH_PLUGIN=mysql_native_password（固定不变）

# 项目端口（部署时 Render 会自动分配，本地可填3000）
PORT=3000

# ========== 可选：如果集成了 Cloudinary 图片存储 ==========
# CLOUDINARY_CLOUD_NAME=你的Cloudinary云名称
# CLOUDINARY_API_KEY=你的Cloudinary API Key
# CLOUDINARY_API_SECRET=你的Cloudinary API Secret